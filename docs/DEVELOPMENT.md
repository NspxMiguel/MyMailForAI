# Desenvolvimento — MyMailForAI

_Gerado em 2026-08-31._

## Comandos

Nenhum script de package.json / `scripts/` / `build.sh` / `project.yml` detetado.

## Testes

Existe diretório de testes (`Tests/`, `tests/`, `__tests__` ou `test`).

## Build

Sem manifesto de build explícito detetado além do código-fonte.
