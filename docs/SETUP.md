# Instalação e configuração — MyMailForAI

_Gerado em 2026-08-31 a partir de manifests reais._

## Pré-requisitos

## Instalação

```bash
cd /Users/miguel/Documents/Claude/Projetos/MyMailForAI
```

## Variáveis de ambiente

Não foram encontrados ficheiros `.env.example` / `.env.sample` / `.env.template` nem xcconfig com chaves exportadas neste repositório.

> Se a app usa secrets em runtime, documentar nomes (nunca valores) quando existir um exemplo versionado.

## Comandos

Nenhum script de package.json / `scripts/` / `build.sh` / `project.yml` detetado.
